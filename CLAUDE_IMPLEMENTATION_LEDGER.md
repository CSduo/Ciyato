# Ciyato — Claude Implementation Ledger

Tracks every finding from `Ciyato_FINAL_COHESIVE_Master_Audit_2026-08-16.docx` (Revision III)
from confirmation through to verified implementation.

| Field | Value |
|---|---|
| Audited SHA | `5773448cbd2d189055021b319d54c74ae2c779bf` |
| HEAD at start of implementation | `5773448cbd2d189055021b319d54c74ae2c779bf` — **identical**, so pinned locators apply directly |
| Baseline tag | `baseline-audit-5773448` |
| Repository | `CSduo/Ciyato` |
| Implementer | Claude, single context, no agents or delegated model calls |

## Status vocabulary

| Status | Meaning |
|---|---|
| `CONFIRMED` | Located in current source; the defect is real and reproducible from the code |
| `RECLASSIFIED` | Real, but the severity, cause or scope differs from the audit's description — reason recorded |
| `NOT APPLICABLE` | Code has changed or the premise does not hold; evidence recorded, no patch applied |
| `IMPLEMENTED` | Fixed, compiled, tests green |
| `VERIFIED` | Acceptance condition from the audit demonstrably met |
| `BLOCKED` | Needs a device, a Play account, or a decision only the publisher can make |
| `DUPLICATE-OF` | Part VII restatement of an earlier finding; fixed once, evidenced against the primary |

## Root-cause grouping

The audit's own instruction is explicit: *"If two findings point to the same root cause, fix the
root cause once and mark both findings with evidence. Do not make 210 disconnected cosmetic
edits."* Groups are recorded here as they are established, so that a single fix closes every
finding it genuinely resolves.

Initial observation, to be confirmed finding-by-finding: **F-171 through F-210 appear to be
Part VII traceability restatements** of findings in F-001–F-170 rather than 40 new defects — e.g.
F-185 restates F-002 (targetSdk), F-189 restates F-003 (debug signing), F-198 restates F-095
(rolling "today"), F-200 restates F-094 (400 ms delay), F-204 restates F-101 ("Keep Best"),
F-205 restates F-113 (double-counted bytes), F-206 restates F-005 (dead release URL). Each will
be marked `DUPLICATE-OF` only after the restatement is checked against its primary, never assumed.

## Findings

| ID | Severity | Status | Current location | Batch | Evidence |
|---|---|---|---|---|---|
| F-001 | P0 | IMPLEMENTED | `.github/workflows/android-debug.yml` | B0 | Confirmed: `working-directory: ciyato-android` at lines 25/29 and stale APK path at 35; `git ls-files` shows **zero** tracked files under `ciyato-android/`, and `gradlew` is at the repo root. Every run died before Gradle, so no commit had automated proof of compilation. Workflow rewritten: stale working-directory removed, root `gradlew` used, and `testDebugUnitTest` + `lintDebug` + `assembleDebug` now run as separate named steps (previously only `assembleDebug` ran, so tests and lint were never executed at all). Reports upload on failure. Pending: green run on GitHub, then mark as required check. |
| F-002 | P0 | CONFIRMED | `app/build.gradle.kts:10,15` | — | `compileSdk = 34`, `targetSdk = 34`. Play submission requirement moves to API 36 on 31 Aug 2026. Scheduled for Phase 1. |
| F-003 | P0 | CONFIRMED | `app/build.gradle.kts:59` | — | `signingConfig = signingConfigs.getByName("debug")` inside the release block. Release artifacts are debug-signed. Phase 1. |
| F-007 | P0 | CONFIRMED | `data/FileAccess.kt:85-89` | — | `shareableUri` ends `.getOrDefault(uri)`, returning the raw `file://` URI when `FileProvider.getUriForFile` throws — i.e. it fails **open**, defeating the helper's entire purpose. Note: this line is my own work from 16 Aug; the audit caught a defect I introduced. Fix is the sealed `ShareableUriResult` in the audit's recipe C so callers must handle failure. |
| F-006 | P1 | IMPLEMENTED | `res/xml/file_paths.xml` + `data/FileAccess.kt` | B2 | RECLASSIFIED in approach: narrowing `<external-path>` would break opening ordinary files, since a file manager must reach wherever the person browsed. The constraint moved to the only code that mints these URIs — `shareableUri` canonicalises (resolving `..` and symlinks) and refuses anything outside shared storage or Ciyato's own dirs. Reasoning recorded in the XML so a later edit doesn't undo it. |
| F-007 | P0 | IMPLEMENTED | `data/FileAccess.kt` | B1 | Was `.getOrDefault(uri)` — failed **open**, returning the raw `file://` URI the helper existed to eliminate. Now a sealed `Shareable` result the compiler won't let a caller ignore. All five call sites delegate to a single new owner, `FileAccess.openExternally`, which also folded in three drifted copies of the same intent logic. **My own regression from 16 Aug.** |
| F-009 | P0 | IMPLEMENTED | `data/PhotoBackupWorker.kt` | B3 | Destination was `Ciyato_Backup_<timestamp>`, new folder per run, so any photo seen twice was copied twice into two places. One reused folder + per-file existence check (names read once, not a query per file). Re-running is now genuinely a no-op. |
| F-010 | P0 | IMPLEMENTED | `data/PhotoBackupWorker.kt` | B3 | `done++` sat outside the stream blocks; either stream can be null, so `?.use` was skipped silently and the count still rose — "200 photos saved" with zero bytes written, then the watermark advanced past all 200. Counts only after bytes move and flush; failed copies delete their empty placeholder (otherwise the next run skips the name forever); watermark advances only on a complete run; worker returns `retry()` not `failure()`. 8 regression tests. |
| F-012 | P1 | IMPLEMENTED | `data/DuplicatePhotoDetector.kt` | B4 | Cap kept (hashing is decode-bound) but no longer hidden: `SCAN_LIMIT` is public and `DuplicateScan` carries `scanned`/`libraryTotal`/`wasBounded`, which the UI states. |
| F-013 | P1 | IMPLEMENTED | `data/DuplicatePhotoDetector.kt` | earlier | Threshold 10→5 of 64 bits; renamed `pHash`→`averageHash` because there was never a DCT. Documented as the weaker thing it is. |
| F-022 | P0 | IMPLEMENTED | `ui/screens/OnboardingScreen.kt` | B2 | "Everything stays on this phone. No accounts, no uploads." was false — weather sends coarsened location to 3 hosts, breach check sends a hash prefix. Now local-first and names both exceptions. |
| F-023 | P0 | IMPLEMENTED | `ui/screens/SafeBrowsingHelperScreen.kt` | B2 | `endsWith` matched raw characters, not DNS label boundaries, so `evilgoogle.com` was declared safe. `hostMatchesDomain` requires a label boundary. A host merely *containing* a famous name is now itself a warning sign. |
| F-024 | P0 | IMPLEMENTED | `ui/screens/SafeBrowsingHelperScreen.kt` | B2 | The type could express a verdict it had no basis for: no heuristic hit → `Safe` → "No threats detected. Safe to open." `UrlCheck` replaces it and **cannot say safe**; signals are collected not short-circuited; the no-signal case states explicitly that no reputation service was contacted. |
| F-059 | P2 | IMPLEMENTED (partial) | `ui/screens/SafeBrowsingHelperScreen.kt` | B2 | Emoji removed from security verdicts — "✅ URL is Safe" reads as "white heavy check mark" or is skipped. Other screens still to sweep. |
| F-098 | P1 | IMPLEMENTED | `ui/screens/NlFileSearchScreen.kt` | B1 | Open was wrapped in a bare `runCatching` that discarded the exception, so an unopenable file looked like a dead row. Now reports the reason. |
| F-100 | P0 | IMPLEMENTED | `ui/screens/DuplicatePhotoCleanupScreen.kt` | B4 | Card rendered `take(3)`, button deleted `size - 1` — a group of 8 destroyed 5 photos never shown. All photos displayed; `trashAllExcept(group, keep)` takes the survivor as an argument so code and UI cannot drift again. |
| F-101 | P1 | IMPLEMENTED | `ui/screens/DuplicatePhotoCleanupScreen.kt` | B4 | "Keep Best" equated largest file with best photo. Survivor is now tappable, defaulted to largest, each candidate's size shown so the default is verifiable. Button reads "Trash N". |
| F-102 | P1 | IMPLEMENTED | `ui/screens/DuplicatePhotoCleanupScreen.kt` | B4 | "No duplicates found!" was a whole-library claim from a 500-photo scan. Every count now qualified with real coverage. |
| F-103 | P1 | IMPLEMENTED | detector + cleanup screen | B4 | Vocabulary separated: "look-alike", "Similar is not identical — check each group before trashing". Nothing claims proof of duplication, which an average hash cannot give. |
| F-163 | P1 | CONFIRMED | `ui/screens/OnboardingScreen.kt` | — | Confirmed **visually** on API 36: "AI Phone Organizer" appears twice in the very first frame a new user sees. |
| — (lint) | P0 | IMPLEMENTED | `res/values/themes.xml` → `res/values-v27/` | B1 | `android:windowLightNavigationBar` is API 27, `minSdk` 26 — a lint **error**, so the build failed. Real hazard: on API 26 that attribute id is unclaimed and OEMs have reused unclaimed ids. Moved to a versioned folder. |
| — (new) | P2 | CONFIRMED | onboarding preview card | — | Found by screenshot, not source: the preview card clips its own content — "Smart categories" cut mid-card by the pager edge, "Organized apps" sliced, page dots overlaying the clipped area. |

Remaining findings are appended as each is located and confirmed against current source, rather
than pre-filled from the document — an unverified row in this table would be exactly the kind of
false completion signal the audit warns about.

## Newly discovered defects (not in the audit)

Recorded here when implementation uncovers something the audit did not list, per the "no blind
checklist patching" rule.

| ID | Severity | Status | Location | Notes |
|---|---|---|---|---|
| N-01 | **P0** | IMPLEMENTED (B22) | `ui/components/CiyatoInputs.kt` | **A password field that never masked its password.** `CiyatoPasswordField` set a password keyboard and drew a reveal toggle, but `CiyatoInputField` had no `visualTransformation` parameter, so the text rendered in the clear at all times and `showPassword` only chose which eye icon to draw. Both `PasswordVisualTransformation` and `VisualTransformation` were already imported in that file and never referenced — the masking was intended and lost. It had not shipped a visible password only because nothing called it. Parameter added to the primitive; the field now masks; the real screen was moved onto it so it cannot go dormant again. |
| N-02 | P1 | IMPLEMENTED (B22) | `ui/screens/DataBreachCheckerScreen.kt` | The HIBP request omitted `Add-Padding`, so every prefix returned a differently-sized response and an on-path observer could narrow the queried prefix from the byte count — while the screen told the user the check was privacy-safe. k-anonymity protects the hash; padding protects the query, and the claim needs both. Header added, plus the consequence it carries: padded responses contain synthetic hashes with a count of zero, which would otherwise have been reported as "breached 0 times". |
| F-128 | **P1** | IMPLEMENTED (B47) | `data/UsageAnomalies.kt` (new), `ui/screens/AnomalyDetectionScreen.kt` | The z-score compared `times.last()` — **the day in progress** — against the mean of the complete days before it. At 9am today holds two hours while the history holds full days, so nearly every app scored a large negative z and the screen announced a wave of "unusual drops" that were nothing but the clock. The subject is now the last **finished** day. 10 tests. |
| F-129 | P1 | IMPLEMENTED (B47) | `ui/screens/AnomalyDetectionScreen.kt` | A thrown query returned `emptyList()`, and an empty list rendered as a green tick and "Your usage patterns look consistent" — **a failed analysis presented as a healthy conclusion**. Four distinct outcomes now: unavailable (with the reason), not-enough-history, nothing-unusual (with how many apps were actually judged), and findings. The green tick is reserved for an analysis that ran. |
| F-123 | P1 | IMPLEMENTED (B47) | `ui/screens/ContextualSuggestionsScreen.kt` | The "confidence" percentage was `(usageHours / 2f).coerceIn(0.4f, 0.95f)` — a rescaled duration wearing the costume of a probability. There is no model and no prediction, so there was nothing to be confident about; the percentage only implied uncertainty had been measured. Shows the hours instead: same information, no false precision. |
| F-174 | P1 | IMPLEMENTED (B47) | `ui/screens/FocusSessionScreen.kt` | The setup card promised "Apps are still accessible from the full drawer". `launchApp` refuses a blocked category regardless of where the tap came from, and the drawer uses that same function — so someone relying on the drawer as an escape hatch found it closed. The behaviour is the better one, so the copy changed, and it names the real way out (end the session; other launchers and notifications are unaffected). |
| N-19 | **P0** | IMPLEMENTED (B46) | `data/VaultCrypto.kt`, `ui/screens/SecureFileVaultScreen.kt` | **Found by independent security review, not by me.** Interrupted writes were siblings of real vault files, told apart by `name.contains(".vaulttmp")` — and vault names come from the imported file, unsanitised. So importing anything called `notes.vaulttmp.txt` created a real vault file the sweep classed as disposable: hidden from the list right after a "successful" import, then **deleted outright on the next unlock**, skipping the confirmation the real delete path requires. Exactly the misclassification F-016 hardened `verifyAndMigrate` against — one layer up, over user-controlled input, which is where I had stopped looking. Staging now has its own directory, so a real file can never be in it whatever it is called. |
| N-20 | **P1** | IMPLEMENTED (B46) | `ui/screens/SecureFileVaultScreen.kt`, `data/VaultCrypto.kt` | Also from the review: import built its destination straight from the filename, and `writeAtomically` ends in `renameTo`, which replaces its destination silently. Importing a second `IMG_20240101_120000.jpg` **destroyed the first one's ciphertext** with no warning and nothing to recover from. Camera, screenshot and download names collide constantly — ordinary use, not an edge case. Destinations are now disambiguated, and import names sanitised as defence in depth. 12 tests. |
| F-082 | P1 | IMPLEMENTED (B46) | `ui/screens/DashboardScreen.kt` | Dashboard used the same boolean that answers true for Android 14's partial grant, so every category count described the hand-picked subset while being presented as the device's library — the F-115 defect in a second place. Reuses the `MediaAccess` owner built for that fix. |
| F-084 | P1 | IMPLEMENTED (B46) | `ui/screens/DashboardScreen.kt` | The storage ring is whole-partition usage from `StatFs` (no permission, includes the OS and every app); the category counts beneath it are permission-limited MediaStore queries. They can never sum to it, and under a partial grant are a subset of a subset — presented side by side as if commensurable. The card now says which is which. |
| F-190 / F-001 | **P0** | IMPLEMENTED (B45) | `.github/workflows/android-ci.yml` (new), `android-debug.yml` (deleted), `app/build.gradle.kts` | **CI has never passed in this repository.** Every step set `working-directory: ciyato-android` — a folder that does not exist here, the app module is at the root — so the workflow died before reaching Gradle on every push since the baseline. A build that has always been red teaches people to ignore the badge, which is worse than no CI. The replacement runs from the real root and gates on unit tests, `lintDebug` (fails on errors), `assembleDebug` and **`bundleRelease`** — release-only breakage from R8, resource shrinking and manifest merging is invisible to a debug build. Needed a deliberate `-PciyatoAllowUnsignedRelease=true` opt-out, since the signing guard correctly refuses to build a release without an upload key; verified locally first, because shipping CI that fails forever is the bug being fixed. |
| F-161 / F-037 | P1 | IMPLEMENTED (B44) | `ui/theme/Type.kt` | The file said "Uses Inter as primary font (bundled in assets)" and described weight-variant registration for four weights. **There is no `res/font` directory and no font in assets** — every alias is `FontFamily.SansSerif`, i.e. the platform face. Three documents therefore described a typography system the app does not have. The aliases are kept (they are a genuine single switch point) but now say what they are, with the exact steps to actually bundle Inter. |
| F-159 / F-160 | P1 | IMPLEMENTED (B44) | `ui/screens/ThemeStudioScreen.kt` | "Live launcher preview" was neither live nor the launcher: grey boxes for tiles, a hard-coded "Good morning", a rounded rectangle for the dock. It reflects exactly two settings — density and accent — while sitting on a screen that also changes wallpaper, icon shape, grid size and font, so it implied those were previewed too. Renamed to "Layout sketch" and states what it does and does not show. Rendering real Home at thumbnail scale is the better end state and is a much larger change. |
| F-088 | P1 | IMPLEMENTED (B43) | `ui/screens/FilesScreen.kt` | The refresh handler supported an all-files rescan and **said so in a comment**, while the icon was gated on a SAF root existing — so the one mode where stale state is most likely (a broad scan of the whole device) was the only mode with no way to refresh. The code and the UI contradicted each other two hundred lines apart. |
| F-091 | P1 | IMPLEMENTED (B43) | `ui/screens/FileCollectionDetailScreen.kt` | The folder card claimed "No broad storage access" — true on the SAF path, false on the other, since Ciyato offers All files access and this same browser uses it when granted. A permission claim that is true on one code path and false on another is worse than vague copy. It now states which mode is actually active. |
| F-099 | **P1** | IMPLEMENTED (B43) | `ui/screens/NlFileSearchScreen.kt` | In all-files mode search reads an index that **Files** builds. An unbuilt index produced an empty result set, which renders as "No files found — try a different query" — so a hidden prerequisite across two screens looked like a failed search, and the person rephrases forever while nothing is being searched at all. The two empty states are now distinct, and the recovery is offered inline with a required `onOpenFiles` rather than left to be guessed. |
| F-202 | P1 | IMPLEMENTED (B43) | `ui/screens/RecentFilesScreen.kt` | The rationale asked for image, video and audio permissions and promised "your recently modified files across the device" in the same sentence — a scope those permissions cannot deliver, since documents and archives are invisible through them. Copy now names the dataset (recently modified **media**), and the empty state no longer implies the device has been idle. |
| F-141 | **P1** | IMPLEMENTED (B42) | `ui/screens/VoiceCommandScreen.kt` | `"open <app>"` matched against `viewModel.searchResults` — whatever the person last typed into the **search screen**. So "open Gmail" worked only if Gmail happened to be sitting in a stale result set, and otherwise fell through to "Searching for gmail…", which reads as the command being unsupported rather than mis-wired. Now matches the installed app list, exact label then prefix then substring, so "open maps" reaches Maps rather than whichever app merely contains "maps". |
| F-140 | P1 | IMPLEMENTED (B42) | `ui/screens/VoiceCommandScreen.kt` | "show photos" was listed in the help text and in the screen's own examples, and the handler had no branch for it — saying it produced "Command not recognized" next to a screen advertising it. Implemented rather than removed, and `onOpenPhotos` is a **required** parameter: a defaulted no-op is how a feature comes to be advertised and do nothing. |
| F-143 | **P1** | IMPLEMENTED (B42) | `ui/screens/VoiceCommandScreen.kt` | The recogniser was destroyed only from `onResults`/`onError`. Leave the screen mid-listen and neither fires: it leaked, and **the microphone stayed open on a screen the person had already left**, with only the system mic indicator to show it. There was no deliberate way to stop it either. Release is now tied to the composable's lifetime via `DisposableEffect`, plus an explicit `stopListening()`. |
| F-137 | P1 | IMPLEMENTED (B42) | `ui/screens/WidgetHostScreen.kt` | Every third-party widget was forced into a fixed 120dp card. A clock designed for 40dp floated in empty space; a 4x2 calendar was cut off. Height now derives from `AppWidgetProviderInfo.minHeight`, clamped to 48–320dp against absurd or unbounded values, and `updateAppWidgetSize` tells the widget the box it actually got so it can choose the right layout. |
| F-062 | **P1** | IMPLEMENTED (B41) | `ui/screens/PhotosLibraryScreen.kt` | On API 30+ Android shows its own consent dialog and the removal is reversible from the system trash. On API 26-29 there is neither — and the legacy path deleted **immediately with no confirmation at all**, then toasted "Deleted 12". Ciyato shows a Trash tab on newer devices, so "delete" reads as reversible unless the screen says otherwise. A confirmation now states that this Android version has no system trash and there is nothing to restore from. |
| F-061 | P1 | IMPLEMENTED (B41) | `ui/screens/FilesScreen.kt` | `wasBounded` was surfaced, but as a footnote: "No verified duplicates were found ... The analysis was capped for safety" reads as an answer plus a caveat, and people take the answer. A bounded scan now **leads** with "Partial scan", gives inspected and compared counts, says why the cap exists, and suggests a smaller folder. The unbounded result says plainly that all candidates were compared. |
| F-011 | P1 | IMPLEMENTED (B41) | `data/PhotoBackupWorker.kt` | Periodic backup required only storage-not-low, but a SAF destination is whatever folder was picked — Drive or an SMB share as easily as internal storage — so a daily library copy could run over metered data or fail repeatedly offline. Constraints now follow the destination: battery-not-low always, unmetered network only when the provider authority is not demonstrably local. Requiring a network unconditionally would break backup to internal storage in flight mode. Also `KEEP` -> `UPDATE`, or repointing the folder would leave yesterday's constraints in place. 6 tests. |
| F-005 | P1 | IMPLEMENTED (B41) | `app/build.gradle.kts` | `GITHUB_RELEASES_URL` pointed at `github.com/ciyato/launcher` — a different project from `CSduo/Ciyato` — and **nothing in src/ read it**. Removed rather than corrected: a dead constant naming a plausible endpoint tells the next maintainer an update check exists. |
| F-065 | P1 | IMPLEMENTED (B41) — owner may change | `LICENSE` (new) | No licence anywhere in the tree, which is not a neutral state: default copyright applies but nobody can tell it was intended. Chose **all-rights-reserved as the reversible option** — a permissive licence cannot be withdrawn once published, so starting closed preserves every choice. The file documents the Apache-2.0 / MIT / GPL-3.0 alternatives and what each forecloses, so switching is a one-line decision for the owner. |
| N-18 | P2 | IMPLEMENTED (B41) | `data/PhotoBackupWorker.kt` | Caught by a failing test: the first cut of `isLikelyNetworkBacked` used `Uri.parse`, which is a **non-functional stub in JVM unit tests** — so it reported every destination as local under test. Production behaviour was correct; the test proved nothing. Authority is now parsed from the string, which needs no framework and is genuinely verifiable. |
| F-060 | **P1** | IMPLEMENTED (B40) | `data/FileCleanupWorker.kt` | The resume checkpoint was **URI -> hash**, and the scan loop skipped any URI it already held. A URI is not an identity: a document edited in place between a cancelled scan and its resume kept its **old** hash, and could then be grouped with a file that genuinely has that content — inside a flow whose next step is deleting the duplicate. Entries now carry `{hash, size, lastModified}` and are reused only when both still match; a provider reporting no modification time makes the entry unusable, because re-hashing costs time and a wrong duplicate costs a file. 7 tests. |
| F-054 | P1 | IMPLEMENTED (B40) | `data/CrashReporter.kt` | `loggingEnabled` defaulted to **true** while the opt-out arrives asynchronously from DataStore, so a crash in the startup window was persisted for someone who had explicitly disabled diagnostics. Now fails closed. The trade is stated in the code: starting closed loses an early crash for people who opted in; starting open writes a file for people who opted out, and only one of those is a privacy failure. |
| F-025 | P1 | IMPLEMENTED (B40) | `ui/screens/DataBreachCheckerScreen.kt` | "Privacy-safe check" overstated a k-anonymity lookup that is still a network request — now "privacy-preserving lookup", the audit's own wording. The not-found result also said only "Not found in any known breach database"; absence from one corpus is not proof of strength, and it now says so, because a false reassurance is one someone acts on. |
| F-035 | P1 | IMPLEMENTED (B40) | `services/CiyatoNotificationListenerService.kt` | The badge was documented as an "unread count". Android's listener reports only which notifications are currently posted: an app posting one summary for forty messages reports one, a read-but-undismissed notification still reports, and an app that never posts reports nothing however much waits inside it. There is no universal per-app unread truth to read, so the docs and API no longer claim one. |
| F-032 | **P1** | IMPLEMENTED (B39) | `data/ForecastClock.kt` (new), `data/WeatherRepository.kt` | The request asks Open-Meteo for `timezone=auto`, so every returned stamp is local time **at the forecast location** — and the code matched them against `Calendar.getInstance()` in the **device's** zone. Those agree only when the two zones match. Worse, the no-match fallback was `?: 0`, and index 0 is **midnight of the first forecast day**, so "current conditions" showed the middle of last night with no sign of trouble. Now derived from the response's `utc_offset_seconds`, falling forward to the next hour ahead rather than to zero. 11 tests, each with the device zone deliberately set to something other than the forecast's. |
| F-031 | P1 | IMPLEMENTED (B39) | `data/WeatherRepository.kt` | Forecast, then AQI, then reverse geocode — serially. A slow air-quality endpoint delayed a forecast that had already arrived, so the weather system felt broken because an optional service was having a bad day. AQI and geocode now run concurrently under `coroutineScope`, each with independent failure handling. |
| F-030 | P1 | IMPLEMENTED (B39) | `data/LocationFreshness.kt` (new), `data/LocationHelper.kt` | `getLastKnownLocation` has no upper bound on age and any cached fix was accepted. Ranking providers by age is not rejecting a stale answer: when every cache is a day old, the freshest is still a day old. Land after a flight and Home named the city you left, beside a temperature. 30-minute age gate plus a deliberately loose 50 km accuracy gate (coordinates are coarsened to ~1.1 km anyway, so the gate rejects the pathological case, not the normal coarse one). A zero or future timestamp is treated as unusable rather than as "now". 12 tests. |
| F-016 | **P1** | IMPLEMENTED (B38) | `data/VaultCrypto.kt` | **The most dangerous line in the vault.** `verifyAndMigrate` treated "not the current format" as evidence of the legacy format, so a modern file whose first two bytes were damaged would be XOR-"decoded" and re-encrypted over itself — destroying the only copy of the plaintext while reporting success. A file carrying the magic byte is now recognised as ours and left alone if its version is unknown; legacy migration additionally proves a full encrypt-decrypt round trip **before** anything replaces the original. `writeAtomically` also `fsync`s before rename — without it, rename can be durable while the data is still in the page cache, so power loss leaves a file that exists and is empty. |
| F-018 | **P1** | IMPLEMENTED (B38) | `ui/screens/SecureFileVaultScreen.kt` | `isUnlocked` survived for the composable's lifetime, so a vault opened once stayed open — press Home, hand over the phone, and returning through Recents showed the decrypted file list with no prompt. Re-locks on `ON_STOP`, clears the decrypted names, and deletes the plaintext copies written for "open in another app". `ON_STOP` not `ON_PAUSE` deliberately: pause fires for the biometric prompt itself, which would re-lock the screen mid-unlock. |
| F-017 | P1 | **MITIGATED** (B38) — streaming still open | `data/VaultCrypto.kt`, `ui/screens/SecureFileVaultScreen.kt` | Whole-file encryption holds plaintext, ciphertext and Cipher's copy at once, so a large video costs several times its size in heap and can fail mid-security-operation. Streaming AEAD is the real fix and a larger change. Until then a 64 MB limit is **enforced before the file is read** and explained in the message, rather than discovered as an OOM. 7 tests, including that the limit stays under a third of a typical heap. |
| F-015 | P1 | **OPEN — recorded, not attempted** | `data/VaultCrypto.kt` | The Keystore key is not `setUserAuthenticationRequired`, so the unlock boundary is the Compose gate rather than the key itself. Doing it properly needs a `BiometricPrompt` `CryptoObject` flow, and rotating the key alias would orphan every existing vault file. That is a migration to design and verify on hardware, not to attempt blind. |
| F-026 / F-027 / F-028 / F-029 | P1/P2 | IMPLEMENTED (B37) | `ui/screens/PhotosToPdfScreen.kt` (new), `data/PdfPageLayout.kt` (new), `DocumentScannerScreen.kt` (deleted) | Four findings in a screen with **no entry point anywhere**. Three were defects, the fourth was its name. It called itself a scanner while capturing via `TakePicturePreview()` — documented as a small preview **thumbnail** — so text-heavy pages exported unreadable; there was no edge detection, perspective correction or contrast either. Renamed to what it does. **F-027**: full-resolution decode with the bitmap's pixel dimensions as page size (~48 MB heap per 4000x3000 photo, and every page a different physical size). Bounds read first, decode sampled, every page A4 with the image fitted inside margins; one bitmap alive at a time. **F-028**: wrote to `getExternalFilesDir` — app-private, hard to find, deleted on uninstall — while reporting success; now `ACTION_CREATE_DOCUMENT`. **F-029**: pages were bare URIs, so `filter { it != uri }` removed *every* page sharing that URI; pages have identity and explicit move up/down (usable with TalkBack, per F-048). 13 tests on the geometry and decode bounds. |
| F-115 | P1 | IMPLEMENTED (B36) | `data/MediaAccess.kt` (new), `ui/screens/StorageCleanupScreen.kt` | `hasMediaPermission()` answers **true for Android 14's partial grant**, where MediaStore reports only the hand-picked subset. The breakdown subtracted those undersized media totals from whole-device used space and labelled the remainder "Other / app data" - so under a partial grant every photo the person did not select was reclassified as app data, in a chart that looked authoritative. Access is now a three-state value carried *with* the numbers, so the chart cannot be drawn without its caveat; the residual reads "Not visible to Ciyato" and says most of it is likely their own media. |
| F-118 | P1 | IMPLEMENTED (B36) | `ui/screens/StorageCleanupScreen.kt` | Six categories presented as peers invited one mental action - delete - across evidence as different as Ciyato's own regenerable cache and the Downloads folder. Now three tiers, safest first: **Safe to clear** (cache, empty files, trash - regenerable or provably empty), **Worth reviewing** (old screenshots), **Look before deleting** (large files, Downloads - "only a signal, not a verdict"). Downloads' description warns it may hold the only copy. 8 tests pin the assignments, because the cost of a wrong tier is someone deleting a needed file on Ciyato's encouragement. |
| N-17 | P2 | IMPLEMENTED (B36) | `data/MediaAccess.kt` | Partial-access detection existed but was `private` inside `PhotosLibraryScreen`, so `StorageCleanupScreen` re-derived a weaker boolean and got F-115 wrong. One owner now, shared by both. |
| F-059 | P2 | IMPLEMENTED (B35) | `DataBreachCheckerScreen.kt`, `PrivacyDashboardScreen.kt` (deleted) | Security verdicts carried meaning in emoji - warning/check glyphs on "Password Compromised"/"Password Safe", and on the risk tiers. A glyph is the least reliable place for a security result: TalkBack skips it or reads "warning sign", it renders differently per OEM, and it carries nothing for anyone who cannot separate red from green. Verdicts are words now, with the result row's semantics merged so it announces as one sentence. `SafeBrowsingHelperScreen` was already done. |
| F-152 / F-153 / F-156 | P1 | IMPLEMENTED (B35) | tree-wide | Reachability sweep: **7 screens and 7 widget components with no entry point**. One (`SecureFileVaultScreen`) **wired up rather than deleted** - see the removal ledger. |
| N-16 | **P1** | IMPLEMENTED (B35) | `SettingsScreen.kt`, `MainActivity.kt`, `LauncherHomeActivity.kt` | **The secure vault was unreachable** while `STORE_READINESS.md` and `data_extraction_rules.xml` described it as shipping - both written by me one commit earlier, documenting a feature nobody could open. Reachable from Settings in both shells now. |
| F-004 | P1 | IMPLEMENTED (B34) | `app/build.gradle.kts` | `versionCode` sat at 1 across the product's whole evolution. Now derived: `YYMMDDn` via `ciyatoVersionCode()` — strictly increasing by construction, no shared counter, and the number states when the build was cut. A malformed date fails the build, because a version code cannot be corrected after upload. Verified in the built manifest: **2608231**. |
| F-183 | P1 | IMPLEMENTED (B34) | `STORE_READINESS.md`, `app/src/test/.../StoreReadinessDocTest.kt` (new) | The doc claimed "no full-gallery media permission, no broad storage permission, no fine location, no microphone, no calendar … no notification listener" while the manifest declared **every one**. Rewritten from parsed ground truth, and **enforced by a test** rather than trusted: 6 assertions compare the doc against `AndroidManifest.xml` and fail on drift in either direction. |
| N-14 | P1 | IMPLEMENTED (B34) | `AndroidManifest.xml`, `LocationHelper.kt`, `WeatherDetailScreen.kt` | Two permissions removed. **`FOREGROUND_SERVICE`** was declared while the app contains no `startForeground`, no `ForegroundInfo` and no foreground service — unexercisable, and a review liability. **`ACCESS_FINE_LOCATION`** made Android show the Precise/Approximate chooser and preferred the GPS provider, but every coordinate is rounded to ~1.1 km before use — GPS accuracy was collected, paid for in battery, then discarded, while obliging a "precise location" data-safety entry for a benefit nobody received. |
| N-15 | P1 | IMPLEMENTED (B34) | `app/build.gradle.kts` | **The new doc guard was vacuous when written.** It passed after I injected a bogus permission, because the manifest and the Markdown are read from disk at runtime and Gradle tracks neither as a test input — so editing the manifest alone left the test task up-to-date and skipped. Registered both as `inputs.file`, then re-verified by injecting the permission again and watching the build fail without `--rerun-tasks`. |
| F-167 | P1 | IMPLEMENTED (B33) | `ui/theme/Motion.kt` (new), `ui/theme/Animations.kt`, 6 screens/components, both roots | Reduce Motion was a Home-only setting in practice: **9 files ran infinite animations, 2 consulted the preference** — and those 2 were the ones fixed individually, each by threading a `reduceMotion: Boolean` parameter down. That mechanism is why the rest never got it; it required every intermediate composable to forward a flag it had no use for, so the default was always "keep animating". `LocalReduceMotion` is provided once at each Compose root, and `decorativePulse`/`decorativeSweep` suppress the transition entirely rather than animating invisibly. All 4 shared helpers in `Animations.kt` (~7 call sites incl. FocusSessionScreen) are guarded at the source. Each rests at a value that looks deliberate, not broken. The lightning strobe — full brightness twice in under 300 ms — is suppressed outright, since flashing is a photosensitivity concern rather than a taste preference. The breathing guide in Stress-Free Mode is left as essential motion, not decoration. Conversion verified by diffing animated-value names before/after: 4 before, 4 after, none dropped. |
| F-041 | P1 | **PARTIAL** (B32) — feature ViewModels still open | `data/WorkspacePaging.kt`, `data/TimeAwareLayout.kt` (new), `viewmodel/LauncherViewModel.kt` | The acceptance is "focused unit tests without constructing the entire launcher"; the ViewModel needs an `Application`, so nothing in it was testable. Decision logic lifted out and tested (24 new tests). **Still open:** the split into Home/Weather/Files/Photos/Security/Settings ViewModels, and navigation passing IDs rather than the root ViewModel — a lifecycle/ownership change across every screen. |
| N-12 | **P1** | IMPLEMENTED (B32) | `data/TimeAwareLayout.kt`, `ui/screens/SettingsScreen.kt` | **Bedtime mode switched itself off at midnight.** The test was `currentHour >= bedtimeHour`, false at 1 a.m. for a 22:00 bedtime — so hidden apps returned during exactly the hours the feature exists to cover, while Settings said "apps hidden after this time". The window now crosses midnight (chosen hour → 06:00) and the copy states both ends. Survived because the rule read the clock through a property getter, so testing it meant waiting for the right hour. |
| N-13 | P1 | IMPLEMENTED (B32) | `data/WorkspacePaging.kt` | Page↔workspace mapping existed as **three copies of one rule across two files** — two functions in the ViewModel and their inverse in HomeScreen.kt — with nothing asserting the directions agreed. Disagreement would send an edit to the wrong workspace. Unified, with 13 tests pinning the round trip and the boundaries. |
| F-040 | P1 | **PARTIAL** (B31) — monolith body still open | `ui/screens/HomeScreen.kt` -> `HomeCanvas/Backgrounds/Dialogs/Controls/Chrome/Types.kt` | Measured before acting: the `HomeScreen` composable is **2,888 lines / 155 KB — 76% of the 208 KB file**. The 18 satellite functions around it were moved into five concern-named files; HomeScreen.kt is now 164.4 KB (**−21%**). Verified as a pure move, not asserted: diff is **954 deletions / 0 insertions**, and old-vs-new normalised comparison gives **0 code lines lost, 0 non-comment lines added**. Splitting also made the compiler name which private declarations were genuinely shared — collected in `HomeTypes.kt` as `internal`. **Still open:** the 2,888-line body itself (HomeRoute + HomeUiState, HomeHeader/GlanceRow/WorkspacePager/CategorySection/Dock/EditOverlay). It carries drag, drop, edge-flip and canvas-settle; the audit warns local changes here disturb gestures, and its acceptance is a macrobenchmark + recomposition counters, both device-only. Surveyed for the next pass: 9 dialog blocks, 465 lines, closing over only 0–5 locals each. |
| F-058 | P1 | **PARTIAL** (B30) — counted strings done, bulk extraction open | `res/values/plurals.xml` (new), 12 screens | Two habits coexisted: ~7 sites wrote `if (n == 1) "file" else "files"` inline, ~20 wrote `"$n files"` and shipped **"1 files", "1 apps", "1 copies"**. Both are gone — zero bare count-plurals and zero inline English plural rules remain. The migration starts at `<plurals>` on purpose: counted strings are what concatenation gets wrong most reliably, and the only part of F-058 producing visibly wrong output today. The worst case spliced verb agreement mid-sentence (`item${if (n==1) " is" else "s are"}`), which concatenation cannot make translatable. **Still open, measured:** ~505 hard-coded `Text("...")` literals across 56 files; one English `strings.xml`; the app remains unlocalizable until those move. Terminology (the finding's other half) was already correct — "look alike" for average-hash photo grouping vs "verified duplicate" for content-hashed files (F-103) — and is unchanged. |
| F-042 | P1 | IMPLEMENTED (B29) — Room migration still open | `data/CategoryMutations.kt` (new), `data/LauncherSettingsRepository.kt`, `viewmodel/LauncherViewModel.kt` | A category spans **seven** preference keys and every edit wrote each in its own `edit {}`, so rename/merge/delete/undo were seven transactions each — half-applied state on interruption or contention. `editCategories()` reads all seven from one snapshot and writes all seven in a single `edit {}`, closing both the partial-write and read-modify-write races. **Deliberately not a Room migration**: DataStore's `edit {}` is already atomic and serialised, so the storage engine was never the defect — one logical operation split across seven of its transactions was. Room remains the audit's end state and stays open. Two paths also read stale `StateFlow.value` instead of storage; reads now happen inside the transaction. Rules extracted as pure functions, 18 tests. |
| N-11 | P1 | IMPLEMENTED (B29) | `data/CategoryMutations.kt` | Found while writing the F-042 tests: **deleting a category never cleaned the order list or the hidden set**, leaving its name in both pointing at something that no longer existed. |
| F-048 | P1 | IMPLEMENTED (B28) — device verification outstanding | `ui/components/WorkspaceGrid.kt`, `ui/screens/HomeScreen.kt`, `viewmodel/LauncherViewModel.kt` | Home-screen editing was drag-only, so unusable with TalkBack, switch access or a keyboard. **The UI had zero semantics custom actions before this.** Tiles now publish Move (4 directions), Move to page, Resize presets, Remove and Show options, each routed through the callback the drag ends in. Two discoveries: `moveAppWithinWorkspace` takes an ordinal in a reflowed order and repacks the workspace (would have collapsed every deliberate gap) — added cell-precise `moveAppToCell`, with a test asserting the ordinal path *does* compact so the two are never confused; and long-press is a raw pointer detector, so **no long-click action was published at all** and the context menu had no non-touch route. Actions are only announced when they can succeed. TalkBack/switch acceptance run is device-only and stays open. |
| F-071 / F-073 | **P0** | IMPLEMENTED (B27) | `MainActivity.kt`, `ui/screens/DashboardScreen.kt` | Two products both called Home. Took the audit's second sanctioned path - keep the second activity (it holds CATEGORY_HOME, a real Android-role reason) but never label its dashboard Home. Tab is "Overview" on route `overview`; screen is "Ciyato Organizer"; `home`/`dashboard` kept as aliases for pre-rename shortcuts. **Exactly one destination named Home**, which is the acceptance test. |
| F-182 | P1 | IMPLEMENTED (B27) | `MainActivity.kt` | `singleTop` with no `onNewIntent`, so the launcher's Files/Photos/Agenda/Settings intents did nothing whenever the organizer was already open. `onNewIntent` feeds a redirect, and both cold and warm paths resolve through one `routeForDestination()`. |
| F-124 | **P0** | IMPLEMENTED (B27) | `data/UsageAverages.kt` (new), `ui/screens/AiChangelogScreen.kt` | The `associate{}` half was fixed earlier; the divisor was still hard-coded `7`. A phone with three days of history reported averages 2.3x too low, so an ordinary day read as a surge. Days observed are now measured; ratio returns null rather than a misleading zero; nothing is called unusual below three observed days; the copy never says "weekly average" without a week. 11 tests including the audit's seven-entry fixture. |
| F-089 | **P0** | IMPLEMENTED (B27) | `ui/screens/FilesScreen.kt` | Silent no-op fixed earlier, but an enabled control that explains itself only after being tapped still fails the audit's rule. Disabled outside a chosen folder, with the reason beside it. |
| F-090 | **P0** | IMPLEMENTED (B27) | `ui/screens/FileCollectionDetailScreen.kt` | Main bug fixed earlier, but a leftover `selectedFolderUri == null` clause suppressed the grant card exactly when a SAF grant was **revoked** - the screen said "Choose the folder again to continue" and offered no control to do it. Now gates on whether a readable root exists, and the card reads Reconnect rather than Enable. |
| - (lint triage) | P1 | IMPLEMENTED (B26) | tree-wide | The 142 untriaged warnings carried here since 16 Aug were categorised rather than counted. 60 were "a newer version exists" notices carrying no defect. The other 81 collapsed into five causes, all fixed. **141 -> 62 warnings; all 62 remaining are dependency-version notices.** |
| N-06 | P1 | IMPLEMENTED (B26) | `res/drawable/` -> `res/drawable-nodpi/` | 7.5 MB of wallpaper JPEGs sat in the densityless `drawable/` folder, so Android treated them as mdpi and scaled to device density: a 4.2 MB decode becomes 67.6 MB on an xxxhdpi phone, 16x the pixels. Coil downsampling is why this was survivable rather than an OOM. |
| N-07 | P1 | IMPLEMENTED (B26) | `res/mipmap-anydpi/`, `res/drawable-nodpi/ic_launcher_*.png` | Launcher icon mis-authored: logo spanned 65.4% of the canvas against a 61% safe zone **and sat 38px off-centre**, so a circular mask clipped it. Foreground was ink premultiplied onto black rather than a real alpha channel, and there was no `<monochrome>` layer - Ciyato, a launcher, was the one icon that would not theme on Android 13+. Alpha recovered, logo fitted by measured radius to the 66dp safe circle, monochrome generated, verified against circle/squircle/square/themed masks. |
| N-08 | P1 | IMPLEMENTED (B26) | `res/xml/data_extraction_rules.xml`, `res/xml/backup_rules.xml` | `allowBackup="false"` answers the cloud question only. Android 12+ device-to-device transfer defaults to copying everything, and the secure vault is AES-GCM ciphertext under a hardware-bound Keystore key that cannot leave the device - a transfer would have delivered permanently unopenable files while reporting success. Vault and crash logs excluded on both routes. |
| N-09 | P2 | IMPLEMENTED (B26) | `data/ByteFormat.kt` (new) | Four byte formatters disagreed: the same size rendered "1.5 GB", "1.50 GB" or "1.5GB" by screen, one pinned to `Locale.US` regardless of phone language. One implementation now; the previous four delegate, unifying ~40 call sites without editing any. |
| N-10 | P2 | IMPLEMENTED (B26) | `ui/screens/WallpaperPickerScreen.kt` | Header read "Featured 4K Ciyato Wallpapers". Measured: every one is 768x1376 - below 1080p, and below the screen of the phone showing it. |
| F-021 / F-148 | P0 | IMPLEMENTED (B25) | `viewmodel/LauncherViewModel.kt`, `ui/screens/AppLockScreen.kt`, `ui/screens/LockedAppsScreen.kt` (new), `ui/components/AppContextMenu.kt` | **Built, not archived.** `AppLockGate` had no caller, no preference, and no way to mark an app locked. The audit's acceptance test — "every Ciyato launch path shares one policy; copy never implies OS-wide lock" — is met by gating in `launchApp`, which all 16 launch sites already call, so a seventeenth added later is gated by default. Scope is stated before enabling, on the gate, and again on the management screen. |
| N-05 | P1 | IMPLEMENTED (B25) | `ui/screens/ContextualSuggestionsScreen.kt`, `data/WeatherRepository.kt`, `LauncherHomeActivity.kt` | Three launch paths built their own intents and so bypassed **Focus blocking as well as** App Lock — a suggestion card would open an app the rest of Ciyato was actively refusing to open. All now go through `launchPackage()`. `launchSystemWeatherApp` became `findSystemWeatherPackage`, moving the launch decision to the one place that owns it. |
| F-036 / F-142 / F-177 | P1 | IMPLEMENTED (B24) | `ui/theme/Theme.kt`, `ui/theme/MaterialYouSupport.kt`, `ui/screens/VoiceCommandScreen.kt`, `LauncherViewModel`, `LauncherSettingsRepository` | The audit's own prescription — "prefer one honest dark-only contract unless full theme coverage is funded" — applied rather than a half-wired light mode. Both roots hard-coded dark; `viewModel.darkMode` had **zero readers**; the only writers were two voice commands, one of which announced "Light mode enabled." while nothing changed. `CiyatoTheme` no longer takes `darkMode`/`dynamicColor`, the unreachable light and Material You branches are gone, the preference is removed end to end, and the voice commands answer honestly. Full light coverage would mean theming ~150 palette references across every screen — recorded as a real project, not a flag. |
| N-04 | P1 | IMPLEMENTED (B24) | `ui/components/SearchBar.kt`, `ui/screens/AppDrawerScreen.kt` | Found while closing F-036, and shipping: `CiyatoSearchBar` defaulted to a warm cream fill with near-black text for a light drawer that does not exist. AI Search takes the defaults, so it rendered a cream pill inside the near-black screen. The App Drawer overrode all five colours — what a wrong default looks like when one caller has noticed. Defaults are the dark palette now; the drawer's overrides were deleted as redundant. |
| F-077 / F-112 | P1 | IMPLEMENTED (B23) | `data/PhotoDeviceLibrary.kt`, `ui/screens/PhotosLibraryScreen.kt`, `ui/screens/PhotoCollectionsScreen.kt` (deleted) | Two separate photo products, each with its own permission handling, its own partial-access logic, its own collection building. Root cause was the model, not the screens: `PhotoDeviceLibrary` carried photos only, so anything wanting videos had to build a parallel gallery. `DeviceImage` now represents photos and videos alike, `collections()` gained Videos and month buckets — the two things the second screen existed for — and `PhotoCollectionsScreen` is gone, with all three entry points folded into Photos. |
| N-03 | P1 | IMPLEMENTED (B22) | `ui/screens/SettingsScreen.kt` | Bedtime slider called `setBedtimeHour()` on every drag event, so one adjustment wrote to DataStore dozens of times — and "Done" had nothing to confirm, since the value was already saved and dismissing the dialog kept a change the user never accepted. Drag is local; Done commits; Cancel discards. |

## Back-filled rows (verified from source, not from memory)

These were fixed in earlier batches and never got a ledger row. That gap was not
harmless: the register looked less complete than it was, and a future session
could have re-done finished work or, worse, "re-fixed" something and undone it.

Each row below was reconstructed by finding the finding's id annotated in a
source file and quoting the rationale written there — evidence, not recollection.
Anything without code evidence was deliberately left out rather than assumed
done.

| ID | Sev | Status | Location | Evidence quoted from source |
|---|---|---|---|---|
| F-040 | P1 | IMPLEMENTED (back-filled) | `HomeCanvas.kt` | HomeScreen.kt is a ~207 KB interaction monolith — Canvas geometry and the free-placement surface. Split out of HomeScreen.kt, which was a 213 KB file whose single HomeScreen composable accounted for 155 KB of it (F-040). These four are the canvas |
| F-041 | P1 | IMPLEMENTED (back-filled) | `TimeAwareLayout.kt` | LauncherViewModel.kt is a cross-feature god object — Which categories Home features, and whether it is bedtime. Both decisions lived in LauncherViewModel and read the system clock through property getters, so neither could be tested without waiting for the right hour of the day (F-041). Taking the hour as an argument makes them ordinary |
| F-051 | P0 | IMPLEMENTED (back-filled) | `HomeScreenTest.kt` | searchInput_filtersApps instrumentation assertion is vacuous — Instrumentation tests for the launcher Home surface. Rewritten because two of the previous tests could not fail (F-051, F-052): |
| F-052 | P1 | IMPLEMENTED (back-filled) | `HomeScreenTest.kt` | Home app-grid instrumentation test is too generic — Instrumentation tests for the launcher Home surface. Rewritten because two of the previous tests could not fail (F-051, F-052): |
| F-058 | P1 | IMPLEMENTED (back-filled) | `plurals.xml` | Large volumes of user-visible text are hard-coded in Kotlin — plural categories cannot be expressed by it at all (F-058). |
| F-066 | P2 | IMPLEMENTED (back-filled) | `SettingsScreen.kt` | Settings risks becoming a feature catalog rather than a coherent control center — A settings section heading, with an optional caption. The caption exists so experimental features can be labelled as experimental in the place people actually read. Settings previously presented thirteen sections at identical visual weight, so a rough usage heuristic sat beside "Set Ciyato as Home" and looked equally load-bearing (F-066, F-076). |
| F-068 | P1 | IMPLEMENTED (back-filled) | `LauncherHomeActivity.kt` | Manual LauncherDest state is not obviously saveable across activity recreation — Survives activity recreation. This was a plain `remember`, so any configuration change — rotation, font scale, theme, unfolding a device, or the system recreating the launcher after reclaiming memory — silently threw the person back to Home from wherever they actually were (F-068, F-181). Saved as a String key because |
| F-072 | P0 | IMPLEMENTED (back-filled) | `MainActivity.kt` | Many Settings rows become dead taps when Settings is opened from MainActivity — The fourteen below had no route and no callback, so their Settings rows were enabled and inert (F-072). |
| F-076 | P1 | IMPLEMENTED (back-filled) | `SettingsScreen.kt` | Core Settings and experimental utilities are visually flattened into one catalog — A settings section heading, with an optional caption. The caption exists so experimental features can be labelled as experimental in the place people actually read. Settings previously presented thirteen sections at identical visual weight, so a rough usage heuristic sat beside "Set Ciyato as Home" and looked equally load-bearing (F-066, F-076). |
| F-081 | P1 | IMPLEMENTED (back-filled) | `DashboardScreen.kt` | “AI Cleanup” quick action is not AI and simply opens Files — Storage Cleanup. The tile here was labelled "AI Cleanup" with a sparkle icon and simply called onOpenFiles — the same destination as the storage card and the "View all" link, so three controls did one thing and none of them cleaned anything (F-081). There was no AI involved at any point. It now says |
| F-083 | P1 | IMPLEMENTED (back-filled) | `MediaLibraryRepository.kt` | Dashboard collapses query failure, permission denial and true zero into the same count — True when the query could not be answered — denied permission, a provider error, or a null cursor. Without this a failure was indistinguishable from an honest zero: the catch swallowed the exception and the summary returned count = 0, so a screen with no media access showed a confident wall of zeroes rather than saying it could not look (F-083, F-116). |
| F-086 | P1 | IMPLEMENTED (back-filled) | `FilesScreen.kt` | Files category rows do not open the selected category — One Files category. [matches] is held alongside [count] deliberately. The predicate used to live inline inside the count expression, so a row could report "42 Videos" with no way to then produce those 42 files — which is why tapping a category opened an unfiltered browser and silently discarded the choice (F-086). Counting and |
| F-087 | P1 | IMPLEMENTED (back-filled) | `FilesScreen.kt` | “Recent files” rows in Files Home are display-only — A recent-file row that opens the file. These were rendered with no click handler at all, so the most obviously tappable thing on Files Home did nothing (F-087). |
| F-092 | P1 | IMPLEMENTED (back-filled) | `FilesScreen.kt` | APKs can be counted as both Documents and APKs in Files Home — APKs are excluded here because they are also application/* and were therefore counted under BOTH Documents and APKs (F-092). |
| F-094 | P1 | IMPLEMENTED (back-filled) | `NlFileSearchScreen.kt` | Smart File Search intentionally waits 400 ms before searching — The 400 ms sleep that used to be here was theatre — it made a local index lookup feel like remote work (F-094). Removing it is a |
| F-095 | P1 | IMPLEMENTED (back-filled) | `NlFileSearchScreen.kt` | “Today” and “yesterday” in Smart File Search are rolling windows, not calendar days — Calendar days, not rolling windows. "today" was `now - 24h .. now`, so a search at 9am returned files from 3pm YESTERDAY and called them today's (F-095). Nobody means "the last |
| F-096 | P2 | IMPLEMENTED (back-filled) | `NlFileSearchScreen.kt` | Month-name file queries can target a future month in the current year — The most recent occurrence of [month], which is usually what a bare month name means. Two bugs fixed. The year was never set, so the calendar kept the CURRENT year: asking for "December" in August produced a window entirely in the future and therefore always zero results (F-096). And minutes, seconds and milliseconds |
| F-104 | P1 | IMPLEMENTED (back-filled) | `PhotosLibraryScreen.kt` | AI photo organization scans only the newest 250 images by default without leading with tha — Coverage stated on the SUCCESS path too, not only when nothing matched. Labelling is capped at PhotoAiLabeler's maxImages, so results describe the newest N photos — presenting them as the whole library implied a completeness the scan never had (F-104). |
| F-107 | P1 | IMPLEMENTED (back-filled) | `PhotoDeviceLibrary.kt` | Photos Library can say “photos on this device” while silently capping at 3,000 — Default cap on how many images are loaded, newest first. Public because any screen showing a count has to be able to say whether it is looking at the whole library. "N photos on this device" was printed straight from a capped list, so a 12,000-photo library was reported as 3,000 (F-107, F-108). |
| F-108 | P1 | IMPLEMENTED (back-filled) | `PhotoDeviceLibrary.kt` | Photo Collections also presents capped photo/video sets as device-wide — Default cap on how many images are loaded, newest first. Public because any screen showing a count has to be able to say whether it is looking at the whole library. "N photos on this device" was printed straight from a capped list, so a 12,000-photo library was reported as 3,000 (F-107, F-108). |
| F-113 | P0 | IMPLEMENTED (back-filled) | `StorageCleanupScreen.kt` | Storage Cleanup can double-count reclaimable bytes across overlapping categories — Unique bytes, not the sum of category totals. A single file legitimately appears in several categories — a 400 MB video in Downloads is also a Large File, and a trashed photo is also in Trash — so summing category totals counted the same bytes two or three times. The headline could therefore promise more free space than the device physically had (F-113). |
| F-114 | P1 | IMPLEMENTED (back-filled) | `StorageCleanupScreen.kt` | Storage Cleanup labels broad candidate sets as space the user “could free up” — "You could free up" presented a candidate list as guaranteed reclaim, over a total that also double-counted (F-113, F-114). These are things |
| F-116 | P1 | IMPLEMENTED (back-filled) | `MediaLibraryRepository.kt` | Storage scan exceptions silently become zero counts — True when the query could not be answered — denied permission, a provider error, or a null cursor. Without this a failure was indistinguishable from an honest zero: the catch swallowed the exception and the summary returned count = 0, so a screen with no media access showed a confident wall of zeroes rather than saying it could not look (F-083, F-116). |
| F-119 | P0 | IMPLEMENTED (back-filled) | `FocusSessionManager.kt` | Focus UI says distracting apps are “blocked” even though they remain accessible — Whether [category] is currently held back, computed from the clock. Scoped honestly: this only affects launches that go through Ciyato. It is not OS-level enforcement, and copy must not imply that it is (F-119). |
| F-120 | P0 | IMPLEMENTED (back-filled) | `FocusSessionManager.kt` | Focus session lifetime depends on the caller’s coroutine scope and can freeze when started — Focus session logic, derived entirely from a persisted end instant. This used to be a stateful singleton: the session lived in a `MutableStateFlow` inside an `object`, and a 1-second ticker was launched in whatever `CoroutineScope` the *caller* happened to pass in. Three defects followed from that one decision, and all three disappear here rather than being patched: - **The Quick Settings tile could freeze a session forever** (F-120). The tile |
| F-121 | P1 | IMPLEMENTED (back-filled) | `FocusSessionManager.kt` | Focus session is not process-death durable — Focus session logic, derived entirely from a persisted end instant. This used to be a stateful singleton: the session lived in a `MutableStateFlow` inside an `object`, and a 1-second ticker was launched in whatever `CoroutineScope` the *caller* happened to pass in. Three defects followed from that one decision, and all three disappear here rather than being patched: - **The Quick Settings tile could freeze a session forever** (F-120). The tile passed its own service scope, which Android cancels when the tile servic |
| F-122 | P0 | IMPLEMENTED (back-filled) | `InsightsScreen.kt` | Contextual Suggestions claims time-of-day learning without measuring time-of-day behavior — Named for what it measures. It ranks by how often apps are opened; it does not learn time-of-day habits (F-122). |
| F-125 | P1 | IMPLEMENTED (back-filled) | `AiChangelogScreen.kt` | Today Summary adds a fake 600 ms “analysis” delay — A 600 ms sleep used to sit here to make a local UsageStats query feel like "analysis" (F-125). Inventing latency to imply computation is the |
| F-126 | P1 | IMPLEMENTED (back-filled) | `AiChangelogScreen.kt` | Today Summary is based on the last 24 hours, not the local calendar day — The local calendar day, not a rolling 24 hours (F-126). "Today" that |
| F-127 | P1 | IMPLEMENTED (back-filled) | `AppUsageStatsScreen.kt` | App Usage “Today’s total” is also a rolling 24-hour metric — The label says "Today's total", so measure today — local midnight to now — instead of a rolling 24 hours (F-127). The old window meant the |
| F-130 | P1 | IMPLEMENTED (back-filled) | `InsightsScreen.kt` | Four usage-intelligence features should become one coherent wellbeing/insights surface — One entry point for everything built on Usage Access (F-130). |
| F-131 | P0 | IMPLEMENTED (back-filled) | `StressFreeModeScreen.kt` | Stress-Free Mode infers a user’s stress state from trivial usage heuristics — The "Stress Indicator" that stood here has been removed, not softened (F-131). It reported CALM / MILD / STRESSED from two |
| F-135 | P0 | IMPLEMENTED (back-filled) | `SettingsScreen.kt` | Widget manager says widgets are placed on Home, but Home does not host them — Was "Place Android app widgets on your home screen". Home hosts no widgets — there is no AppWidgetHostView anywhere in HomeScreen — so that sentence described a feature that does not exist (F-135). Home placement is the right end |
| F-136 | P1 | IMPLEMENTED (back-filled) | `WidgetHostScreen.kt` | Widgets requiring provider configuration are not configured — Some providers ship a configuration activity and are not usable until it has run — a clock with no timezone chosen, a folder widget with no folder. That step was never launched, so those widgets bound and then rendered empty or default forever (F-136). |
| F-139 | P1 | IMPLEMENTED (back-filled) | `WidgetHostScreen.kt` | Canceling widget bind can leak an allocated AppWidget ID — The ID allocated for an in-flight bind, so it can be reclaimed if the flow does not complete. Without this, cancelling the system bind dialog leaked the ID permanently: result.data is null on cancel, so appWidgetId came back as -1 and the cleanup branch below was never reached (F-139). The old |
| F-146 | P0 | IMPLEMENTED (back-filled) | `SecureFileVaultScreen.kt` | Secure Vault claims files are decrypted “on open,” but the UI has no open/decrypt action — SecureFileVaultScreen — Suggestion #68 Biometric-gated file vault using AES-256-GCM (Android Keystore) encryption. Files are encrypted on import, and can be decrypted and opened again — the screen previously offered no way out at all, which made "vault" the wrong word for it (F-146). |
| F-147 | P1 | IMPLEMENTED (back-filled) | `SecureFileVaultScreen.kt` | Vault file deletion is immediate and has no confirmation/undo — Deleting a vault file destroys the only decryptable copy, so it asks first (F-147). Nothing else in Ciyato deletes user data without confirmation. |
| F-154 | P2 | IMPLEMENTED (back-filled) | `LauncherHomeActivity.kt` | AppUsageStatsScreen appears orphaned while similar usage metrics are exposed elsewhere — object AppUsage : LauncherDest() // Screen Time — was an orphan (F-154) |
| F-155 | P1 | IMPLEMENTED (back-filled) | `NetworkUsageScreen.kt` | NetworkUsageScreen says “current billing cycle” in code but actually queries the last 30 d — NetworkUsageScreen — Suggestion #80 Shows per-app mobile-data usage over the last 30 days. Deliberately NOT "the current billing cycle" — the query is a rolling 30-day window and Android exposes no billing-cycle boundary to apps, so the doc used to describe something the code never computed (F-155). The on-screen copy |
| F-175 | P1 | IMPLEMENTED (back-filled) | `FocusSessionManager.kt` | Focus session lifetime is in-memory while a persisted “active” flag survives it — Focus session logic, derived entirely from a persisted end instant. This used to be a stateful singleton: the session lived in a `MutableStateFlow` inside an `object`, and a 1-second ticker was launched in whatever `CoroutineScope` the *caller* happened to pass in. Three defects followed from that one decision, and all three disappear here rather than being patched: - **The Quick Settings tile could freeze a session forever** (F-120). The tile passed its own service scope, which Android cancels when the tile servic |
| F-176 | P1 | IMPLEMENTED (back-filled) | `CiyatoFocusTileService.kt` | Focus Quick Settings tile ignores the user’s configured duration and category policy — Reads and writes the SAME persisted state the launcher UI uses, and honours the person's configured duration instead of a hard-coded 25 minutes with hard-coded categories (F-176). Because the session is an |
| F-181 | P1 | IMPLEMENTED (back-filled) | `LauncherHomeActivity.kt` | Manual launcher destination is remember-only and is reset by recreation — Survives activity recreation. This was a plain `remember`, so any configuration change — rotation, font scale, theme, unfolding a device, or the system recreating the launcher after reclaiming memory — silently threw the person back to Home from wherever they actually were (F-068, F-181). Saved as a String key because |
| F-191 | P0 | IMPLEMENTED (back-filled) | `AndroidManifest.xml` | QUERY_ALL_PACKAGES requires a core-purpose declaration and sensitive-data disclosure pack |
| F-201 | P1 | IMPLEMENTED (back-filled) | `NlFileSearchScreen.kt` | “Top match” is not a semantic relevance score — Was "Top match", which implies a relevance score. Results are ordered by recency, so the label now says that (F-201). |
